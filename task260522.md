# task260522.md

## 목적

이 문서는 `camera_test` 앱의 2026-05-22 기준 전체 수정 작업 정의서다. 새 대화창에서 이 파일만 읽어도 바로 GitHub 이슈 생성, 코드 수정, 검증, 커밋을 진행할 수 있어야 한다.

핵심 목표는 다음이다.

- 녹화하지 않는 상태에서 앱을 닫아도 백그라운드 서비스, 카메라, 센서가 남아 발열을 유발하지 않게 한다.
- 녹화 중 홈 이동, 백그라운드 전환, 화면 꺼짐 상태에서도 영상이 멈춘 파일로 저장되지 않게 한다.
- Galaxy S25 Ultra 기준에 맞춰 줌, 자동 모드, 비율, 영상 설정, UI를 정상화한다.
- 불필요하거나 실제 미구현인 기능 설명과 설정을 제거한다.
- 작업은 현재 브랜치에서 진행하고, GitHub 이슈를 하나 만든 뒤 모든 커밋 본문에 해당 이슈 번호를 넣는다.

## 현재 작업 환경

- 작업 디렉터리: `c:\Users\HERO\Desktop\camera_test`
- 현재 브랜치: `main`
- 원격 저장소: `origin https://github.com/Won-Hyuk-Lee/camera_test.git`
- GitHub CLI: `gh` 사용 가능
- 현재 `task260522.md` 작성 시점에 문제 정의 문서들은 대부분 untracked 상태다.
  - `README.md`
  - `problem260522.md`
  - `problem260522_claude.md`
  - `problem260522_claude_v2.md`
  - `problem260522_codex.md`
  - `problem260522_codex_v2.md`

## 작업 방식

### GitHub 이슈

코드 수정 시작 전에 GitHub 이슈를 먼저 만든다.

권장 제목:

```text
Fix camera lifecycle, S25 Ultra controls, and background recording stability
```

권장 명령:

```powershell
gh issue create --title "Fix camera lifecycle, S25 Ultra controls, and background recording stability" --body-file task260522.md
```

이슈 생성 뒤 문서 상단 또는 작업 메모에 이슈 번호를 기록한다.

```text
Issue: #번호
```

### 커밋 규칙

사용자 지시 기준으로 현재 브랜치에서 그대로 작업한다. 불필요한 브랜치 전환이나 로그 조회는 하지 않는다.

반드시 지킬 것:

- `git add .` 금지
- 수정 파일을 개별 지정해서 stage
- 작업 단위별로 작게 커밋
- `Co-Authored-By` 금지
- 모든 커밋 본문에 GitHub 이슈 번호 포함

커밋 예시:

```text
Fix: 백그라운드 서비스 잔존 수정

비녹화 상태에서 카메라 화면을 벗어나면 foreground service와 카메라 리소스를 해제하도록 수정합니다.

Issue: #123
```

권장 커밋 단위:

1. `Docs`: 작업 정의 문서와 문제 문서 정리
2. `Fix`: 비녹화 상태 서비스 잔존과 발열 문제 수정
3. `Fix`: 백그라운드 녹화 freeze와 foreground service 타입 수정
4. `Fix`: 볼륨 길게 누르기 종료와 녹화 종료 race 수정
5. `Feat`: S25 Ultra 줌 다이얼과 렌즈 UI 정상화
6. `Feat`: 자동 모드와 프로 모드 분리
7. `Feat`: 비율, 해상도, FPS, HDR 설정 정상화
8. `Fix`: PrivateVault, VideoView, 회전, 설정 저장, lint 보강
9. `Docs`: README의 과장 또는 미구현 설명 정리

## 확정된 사용자 요구사항

- Galaxy S25 Ultra 기준으로 정상화한다.
- 전체 Galaxy 카메라 복제가 목표는 아니다.
- 기본은 자동 모드다.
- 자동 모드는 AF, AE, AWB, 지원 가능한 HDR/상황 보정이 자동으로 동작해야 한다.
- 프로 모드는 별도 모드로 둘 수 있지만 필수 핵심은 아니다. 넣는다면 ISO, 셔터, WB, EV, Focus가 의미 있게 분리돼야 한다.
- 줌은 핀치 줌이 아니라 다이얼/프리셋 방식으로 유지한다.
- 줌 프리셋은 `0.6x`, `1x`, `2x`, `3x`, `5x`, `10x` 기준으로 정리한다.
- mm 단위 물리 렌즈 칩 UI는 제거한다.
- 백그라운드 녹화 종료는 볼륨 업 또는 볼륨 다운을 2초 이상 길게 누르는 방식이다.
- 흔들어서 종료는 사용하지 않는다. 제거하거나 비활성화한다.
- 앱 안에서는 버튼/컨트롤로 직접 녹화를 종료할 수 있어야 한다.
- 촬영 시작/종료 Toast, snackbar, 팝업성 알림은 가능한 한 띄우지 않는다.
- foreground service 알림은 완전히 제거할 수 없으므로 최소화하고 일반 문구로 표시한다.
  - 제목: `동기화 중`
  - 내용: `백그라운드 작업이 진행 중입니다`
  - 종료 액션: `작업 종료`
- 알림 문구가 촬영을 명시하지 않더라도 Android 시스템이나 권한 화면에서 카메라/마이크 사용 자체가 보일 수 있음은 정상이다.
- 셔터 무음 문제는 신경 쓰지 않는다. 사용자가 Galaxy 설정에서 꺼둔 상태다.
- 강제 셔터 무음 우회 구현은 하지 않는다.
- 위치 태그는 설정 화면에 넣고 기본값은 OFF로 둔다.
- 파일 크기 최적화 설정은 제거한다. 실제 구현이 없고 사용자가 제거를 선택했다.
- 빠른 녹화의 의미는 카메라 UI를 거치지 않고 바로 녹화 흐름으로 진입하는 것이다. 클릭 후 녹화 시작까지 0.5초 지연 자체를 버그로 보지 않는다.
- 앱 표시 이름은 `test`로 수정한다.
- 앱 아이콘은 별도 커스텀 아이콘을 만들지 않고 Android 기본 아이콘 기준으로 진행한다.

## 반복하지 말아야 할 오판

이 항목들은 이전 검토에서 정정된 내용이다.

- `CameraController.detachPreview()`에 녹화 중 skip 가드가 이미 반영됐다는 주장은 현재 소스 기준으로 틀렸다.
  - 현재 `detachPreview()`는 `previewUseCase?.setSurfaceProvider(null)`을 호출한다.
- Android 14+ microphone foreground service 설정이 완료됐다는 주장은 현재 소스 기준으로 틀렸다.
  - manifest에는 `FOREGROUND_SERVICE_MICROPHONE`이 없고 service type도 `camera`만 있다.
  - `startForeground()`도 `FOREGROUND_SERVICE_TYPE_CAMERA`만 전달한다.
- `PendingRecording.withAudioEnabled()` 반환값을 버려서 모든 녹화 오디오가 무조건 빠진다는 주장은 CameraX 1.3.4 구현 기준으로 과장이다.
  - 해당 API는 내부 값을 바꾸고 `this`를 반환하는 형태로 확인됐다.
  - 그래도 명확성을 위해 반환값을 변수에 다시 할당하는 스타일로 정리하는 것은 가능하다.
- 빠른 녹화의 0.5초 지연은 현재 합의상 제거 대상 버그가 아니다.
- 강제 셔터 무음은 이번 작업의 목표가 아니다.
- 핀치 줌은 넣지 않는다.

## P0 작업

### P0-1. 앱 종료 후 비녹화 상태 foreground service 잔존 및 발열 수정

현재 구조:

- `CameraFragment`는 카메라 화면 진입 시 `CameraForegroundService.start(ctx)`를 호출한다.
- `CameraForegroundService`는 생성 직후 `CameraController`를 만들고 CameraX 초기화를 시작한다.
- service는 볼륨 리시버와 가속도 센서도 등록한다.
- `CameraFragment.onDestroyView()`는 preview detach와 service unbind만 수행하고 service stop을 보장하지 않는다.
- `CameraForegroundService.onStartCommand()`는 `START_STICKY`를 반환한다.

수정 방향:

- 녹화하지 않는 상태에서는 foreground service가 남지 않게 한다.
- 단순 preview 상태는 Fragment/Activity lifecycle 안에서 처리하고, 녹화 시작 시점에만 foreground service가 필요하도록 구조를 정리한다.
- 당장 구조를 크게 바꾸기 어렵다면 최소한 아래를 보장한다.
  - `onDestroyView()` 또는 적절한 lifecycle 지점에서 `!isRecording()`이면 `CameraForegroundService.stop(ctx)` 실행
  - service 재시작 시 녹화 객체가 없으면 즉시 `stopSelf()`
  - 비녹화 상태에서는 `START_NOT_STICKY` 또는 self-stop 정책 적용
  - `onDestroy()`에서 controller, CameraX, receiver, sensor, wake lock을 확실히 release
- 녹화 중 앱을 백그라운드로 보낸 경우에만 service가 살아남아야 한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`
- `app/src/main/java/com/example/camera2study/CameraController.kt`

완료 조건:

- 카메라 화면 진입 후 녹화하지 않고 뒤로가기/홈/최근 앱 제거를 해도 foreground service 알림이 남지 않는다.
- `adb shell dumpsys activity services com.example.camera2study`에서 비녹화 상태 service가 남지 않는다.
- 비녹화 상태로 화면을 꺼도 장시간 발열이 재현되지 않는다.
- 녹화 중에는 service가 유지되고, 녹화 종료 후에는 service가 정리된다.

### P0-2. 백그라운드/화면 꺼짐 녹화 freeze 수정

현재 문제:

- 촬영 시작 후 폰이 켜진 채 백그라운드로 가는 경우와 전원 버튼으로 화면을 끈 경우 모두 파일은 끊기지 않지만 영상 프레임이 정지된 상태로 저장된다.
- 현재 `detachPreview()`가 녹화 중에도 `previewUseCase?.setSurfaceProvider(null)`을 호출할 수 있어 CameraX 세션이나 surface가 흔들릴 가능성이 높다.

수정 방향:

- 1차 조치로 녹화 중 `detachPreview()`에서 `setSurfaceProvider(null)`을 호출하지 않게 한다.
- 필요하면 Preview use case와 VideoCapture use case 분리를 명확히 한다.
- 화면 OFF와 Fragment view destroy 상황에서 VideoCapture가 살아 있도록 lifecycle을 조정한다.
- `PreviewView`가 SurfaceView 기반으로 동작해 surface destroy가 녹화 세션까지 흔드는지 확인하고, 필요하면 `app:implementationMode="compatible"` 적용을 검토한다.
- 단순히 파일이 생성되는지 말고 실제 프레임이 증가하는지 검증한다.

검증:

- 녹화 시작 후 홈 이동 30초 이상, 화면 꺼짐 30초 이상 각각 테스트
- 저장된 영상에서 화면이 멈추지 않고 실제 프레임이 계속 변화하는지 확인
- 가능하면 `VideoRecordEvent.Status`의 기록 byte 증가와 최종 파일 재생을 함께 확인

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/res/layout/fragment_camera.xml`

### P0-3. Android 14+ foreground service camera/microphone 타입 정리

현재 문제:

- `AndroidManifest.xml`에는 `FOREGROUND_SERVICE_CAMERA`만 있고 `FOREGROUND_SERVICE_MICROPHONE`이 없다.
- service type은 `android:foregroundServiceType="camera"`만 지정돼 있다.
- Android 14+ `startForeground()` 호출도 `ServiceInfo.FOREGROUND_SERVICE_TYPE_CAMERA`만 전달한다.

수정 방향:

- manifest에 `android.permission.FOREGROUND_SERVICE_MICROPHONE` 추가
- service type을 `camera|microphone`으로 변경
- Android 14+ `startForeground()` 호출에서 `FOREGROUND_SERVICE_TYPE_CAMERA or FOREGROUND_SERVICE_TYPE_MICROPHONE` 전달
- 마이크 mute 상태와 상관없이 오디오 포함 녹화가 가능한 기본 정책이면 camera/microphone 병렬 선언이 안전하다.

주요 파일:

- `app/src/main/AndroidManifest.xml`
- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`

완료 조건:

- Android 14 이상에서 백그라운드 녹화 시 카메라와 마이크 foreground service 타입이 모두 선언된다.
- lint 또는 런타임에서 foreground service type 관련 오류가 없다.

### P0-4. 백그라운드 녹화 종료 정책 정리

확정 정책:

- 앱 안에서는 녹화 종료 버튼으로 종료한다.
- 백그라운드에서는 볼륨 업 또는 볼륨 다운을 2초 이상 길게 누르면 녹화 종료한다.
- 흔들어서 종료는 사용하지 않는다.

수정 방향:

- 가속도 센서 기반 shake stop 로직을 제거하거나 비활성화한다.
- service가 센서를 항상 등록하지 않게 한다.
- 볼륨 버튼 short press를 셔터로 쓰는 동작은 백그라운드 종료 정책과 충돌하지 않게 정리한다.
- 종료 직후 `stopSelf()`와 `Recording.Finalize` callback race를 없앤다.
  - `stopBackgroundRecording()` 직후 바로 `stopSelf()`하지 않는다.
  - `Recording.Finalize` callback 안에서 최종 정리와 `stopSelf()`를 수행한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`
- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`

완료 조건:

- 백그라운드 녹화 중 볼륨 업 2초 이상으로 종료된다.
- 백그라운드 녹화 중 볼륨 다운 2초 이상으로 종료된다.
- 흔들어도 녹화가 종료되지 않는다.
- 종료 후 파일 finalize가 끝나고 service가 정리된다.
- finalize 전 service destroy로 인한 dead reference나 파일 손상이 없다.

### P0-5. 촬영 시작/종료 알림 최소화

확정 정책:

- foreground service 알림은 Android 정책상 완전히 제거하지 않는다.
- 촬영이라는 문구를 직접 쓰지 않고 일반 백그라운드 작업 문구를 사용한다.
- 앱 내부 Toast/snackbar/popup성 시작/종료 알림은 가능한 한 제거한다.

문구:

- notification title: `동기화 중`
- notification body: `백그라운드 작업이 진행 중입니다`
- notification action: `작업 종료`

주의:

- 알림 문구를 비우거나 광고성 문구로 위장하지 않는다.
- Android 시스템의 카메라/마이크 사용 표시까지 숨기는 것은 목표가 아니다.

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`

## P1 작업

### P1-1. S25 Ultra 줌 다이얼 정상화

현재 문제:

- 앱에는 `0.6x`, `1.0x`, `2.0x` 버튼이 있으나 실제 초광각/망원 물리 렌즈 전환과 일치하지 않는다.
- 활성 카메라의 `minZoomRatio`가 `1.0`이면 `setZoomRatio(0.6f)`는 `1.0`으로 clamp될 수 있다.
- mm 단위 `lensChipGroup`은 Galaxy 기본 카메라 UX와 맞지 않고 사용자가 원하지 않는다.

수정 방향:

- mm 단위 `lensChipGroup` UI와 관련 코드를 제거한다.
- 줌 프리셋은 `0.6x`, `1x`, `2x`, `3x`, `5x`, `10x`로 정리한다.
- 핀치 줌은 구현하지 않는다.
- 다이얼/프리셋 방식은 유지하되 현재보다 사용하기 편하게 UI를 정리한다.
- 가능한 경우 physical camera 또는 logical camera zoom behavior를 확인해 S25 Ultra에 맞춘다.
- 지원되지 않는 프리셋은 숨기거나 비활성화한다.
- 슬라이더 최소/최대값은 실제 지원 범위와 일치시킨다.

주요 파일:

- `app/src/main/res/layout/fragment_camera.xml`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/java/com/example/camera2study/CameraController.kt`

완료 조건:

- 화면에 mm 단위 렌즈 칩이 없다.
- 사용자는 다이얼/프리셋으로 `0.6x/1x/2x/3x/5x/10x`를 선택할 수 있다.
- 선택된 배율 표시와 실제 카메라 동작이 일치한다.
- 핀치 줌은 없다.

### P1-2. 자동 모드와 프로 모드 분리

자동 모드 요구:

- 기본 모드다.
- 사진 모드에서는 `CONTROL_AF_MODE_CONTINUOUS_PICTURE`를 사용한다.
- 영상 모드에서는 `CONTROL_AF_MODE_CONTINUOUS_VIDEO`를 사용한다.
- `CONTROL_AE_MODE_ON`과 `CONTROL_AE_ANTIBANDING_MODE_AUTO`를 명시한다.
- AWB는 자동으로 둔다.
- 가능하면 중앙 weighted AE/AF metering을 적용한다.
- 지원 단말에서 HDR 또는 scene/extension 기반 보정을 자동 적용한다.
- 단말 정지 감지 시 AF lock 등 헌팅 최소화를 검토한다.
- 수동 설정이 자동 모드에 남아 영향을 주지 않게 reset 정책을 둔다.

프로 모드 요구:

- 별도 모드로 둔다.
- 구현한다면 ISO, 셔터 속도, WB, EV, Focus가 각각 의미 있게 조절돼야 한다.
- 현재처럼 일부 manual parameter만 노출된 상태로 "프로 모드"라고 과장하지 않는다.

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/res/layout/fragment_camera.xml`

완료 조건:

- 자동 모드에서 초점, 노출, 화이트밸런스가 별도 수동 개입 없이 정상 동작한다.
- 사진/영상 모드에 맞는 AF mode가 분기된다.
- 프로 모드가 있다면 자동 모드와 상태가 섞이지 않는다.

### P1-3. 화면 비율과 실제 저장 결과 일치

현재 문제:

- `RATIO_FULL`은 preview layout만 조정하고 실제 출력은 사실상 4:3으로 처리될 수 있다.
- `updateRatioSelection()`이 초기 width 0 상태에서 height 0을 만들 위험이 있다.

수정 방향:

- `Full`의 의미를 명확히 정한다.
  - S25 Ultra/Galaxy 기준에 맞게 preview와 저장 결과가 사용자 기대와 일치해야 한다.
- preview 비율과 실제 사진/영상 output 비율을 가능한 한 일치시킨다.
- 초기 레이아웃 측정 전에는 ratio 계산을 하지 않거나 `post {}` 이후 수행한다.
- ConstraintLayout ratio 또는 명시적 use case target ratio/resolution 전략을 사용한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/res/layout/fragment_camera.xml`

완료 조건:

- `3:4`, `9:16`, `Full` 선택 시 preview와 저장 결과가 납득 가능한 방식으로 일치한다.
- 초기 진입 시 preview가 height 0으로 깨지지 않는다.

### P1-4. 영상 해상도, FPS, HDR 설정 정상화

현재 문제:

- 현재 설정은 CameraX `Quality.UHD/FHD/HD/SD` 수준에 가깝고 S25 Ultra 설정처럼 해상도와 FPS가 분리돼 있지 않다.
- `isFileSizeOptimizationEnabled`는 설정 UI와 변수만 있고 실제 recorder 설정에는 연결되지 않는다.

수정 방향:

- 설정 화면에서 해상도와 FPS를 분리한다.
- 지원 여부에 따라 조합을 제한한다.
- 후보:
  - 해상도: 8K, UHD, FHD, HD 중 지원 항목
  - FPS: 24, 30, 60, 120, 240 중 지원 항목
  - HDR/10-bit: 지원 시만 표시
  - Log Video: 지원 시 고급 영상 설정 또는 프로 모드에 배치
  - 영상 안정화: 지원 시만 표시
- 파일 크기 최적화는 제거한다.
  - 실제 HEVC/H.265 또는 bitrate 정책으로 구현하지 않을 것이면 UI와 변수 모두 제거한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/ui/SettingsBottomSheet.kt`
- `app/src/main/res/layout/bottom_sheet_settings.xml`
- 관련 preference 저장 코드

완료 조건:

- 설정에 실제 동작하지 않는 파일 크기 최적화가 없다.
- 해상도/FPS 선택이 실제 recorder 설정과 연결된다.
- 지원하지 않는 조합은 선택되지 않는다.

### P1-5. UI 사용성 정리

수정 대상:

- 녹화/셔터 UI가 현재 불편하므로 약간 개선한다.
- README가 약속한 "빨간 원 -> 사각형" 녹화 중 변환 애니메이션을 구현하거나 README 약속을 철회한다.
- 사진 모드에서는 마이크 버튼을 `alpha 0.3 + disabled`로 남기지 말고 `View.GONE` 처리한다.
- 영상 모드에서만 마이크 버튼을 표시한다.
- 필요하면 녹화 중 pause/resume 버튼을 별도 P2로 추가한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/res/layout/fragment_camera.xml`
- drawable 리소스
- `README.md`

완료 조건:

- 사진 모드에 영상 전용 마이크 버튼이 보이지 않는다.
- 녹화 시작/중/종료 상태가 직관적으로 구분된다.
- README와 실제 UI 동작이 다르지 않다.

## P2/P3 작업

### PrivateVault 성능 및 리소스 정리

현재 위험:

- `PrivateVaultFragment`에서 파일 목록 조회, 정렬, 이미지/영상 썸네일 decode가 main thread에서 수행될 수 있다.
- VideoView dialog dismiss 시 `stopPlayback()`이 호출되지 않아 MediaPlayer 또는 audio focus 누수 가능성이 있다.

수정 방향:

- 파일 조회와 썸네일 decode를 IO thread 또는 image loader로 이동한다.
- 썸네일 캐시를 적용한다.
- dialog dismiss 시 `videoView.stopPlayback()` 호출을 보장한다.

주요 파일:

- `app/src/main/java/com/example/camera2study/ui/PrivateVaultFragment.kt`

### 설정 저장 및 회전 정리

수정 대상:

- `switchFacing()` 후 `saveCameraSettings()` 호출
- `last_lens_facing` preference 추가
- `Preview.Builder()`와 capture 관련 use case에 `setTargetRotation()` 명시

주요 파일:

- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/java/com/example/camera2study/CameraController.kt`

### WakeLock 및 권한/lint 정리

수정 대상:

- WakeLock 중복 acquire/release 안전성 보강
- 진동을 유지한다면 `VIBRATE` 권한 추가
- 흔들기 종료 제거로 진동도 불필요하면 관련 호출 정리
- `android:tint` 사용 등 lint 오류가 있으면 `app:tint` 등으로 수정
- Camera2 interop 관련 lint opt-in 필요 시 명시

주요 파일:

- `app/src/main/AndroidManifest.xml`
- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`
- 관련 layout XML

### 위치 태그 설정 추가

요구:

- 설정 화면에 위치 태그를 둔다.
- 기본값은 OFF다.
- ON일 때만 위치 권한을 요청하고 사진 EXIF 또는 영상 metadata에 반영한다.

주의:

- 권한 요청 UX와 privacy 설명이 필요하다.
- 이번 작업에서 우선순위는 P1 이후다.

주요 파일:

- `app/src/main/java/com/example/camera2study/ui/SettingsBottomSheet.kt`
- `app/src/main/res/layout/bottom_sheet_settings.xml`
- `app/src/main/java/com/example/camera2study/CameraController.kt`
- permission helper가 있다면 해당 파일

### 앱 이름 및 기본 아이콘 정리

요구:

- 앱 표시 이름을 `test`로 변경한다.
- 런처 아이콘은 별도 브랜딩/커스텀 작업 없이 기본 아이콘으로 진행한다.
- 현재 커스텀 아이콘 리소스가 있다면 기본 launcher icon 사용 상태로 정리한다.

주요 파일:

- `app/src/main/res/values/strings.xml`
- `app/src/main/AndroidManifest.xml`
- `app/src/main/res/mipmap-*`
- `app/src/main/res/drawable*`

### 선택 구현: 영상 pause/resume

요구:

- Galaxy 기본 영상 모드의 일반적인 제어로 pause/resume을 추가할 수 있다.
- CameraX `Recording.pause()` / `resume()` API 사용 가능 여부를 확인한다.
- 반복 녹화 auto-stop 로직과 충돌하지 않게 한다.

우선순위:

- P0/P1 완료 후 여력이 있을 때 진행한다.

## 제외 항목

이번 작업에서 하지 않는다.

- 강제 셔터 무음 우회
- 핀치 줌
- 흔들어서 녹화 종료
- 실제 구현 없는 파일 크기 최적화 옵션 유지
- Galaxy ProVisual Engine과 동일하다는 과장 표현
- Android 시스템의 카메라/마이크 사용 표시 숨기기
- 빠른 녹화의 0.5초 지연만을 이유로 한 구조 변경

## 검증 체크리스트

### 빌드 및 정적 검증

```powershell
.\gradlew.bat assembleDebug
.\gradlew.bat lintDebug
```

실패 시 문서나 최종 보고에 실패 원인과 남은 작업을 명확히 남긴다.

### 기기 수동 테스트

가능하면 Galaxy S25 Ultra 실기기에서 진행한다.

1. 카메라 화면 진입 후 녹화하지 않고 앱을 닫는다.
   - foreground service 알림이 남지 않아야 한다.
   - 카메라/센서가 계속 점유되지 않아야 한다.
   - 발열이 재현되지 않아야 한다.

2. 영상 녹화 시작 후 홈으로 이동한다.
   - 녹화가 계속되어야 한다.
   - 저장된 영상이 정지 프레임이 아니어야 한다.
   - foreground service 알림 문구는 `동기화 중`이어야 한다.

3. 영상 녹화 시작 후 전원 버튼으로 화면을 끈다.
   - 녹화가 계속되어야 한다.
   - 저장된 영상이 정지 프레임이 아니어야 한다.

4. 백그라운드 녹화 중 볼륨 업을 2초 이상 누른다.
   - 녹화가 종료되고 파일이 정상 finalize되어야 한다.
   - service가 정리되어야 한다.

5. 백그라운드 녹화 중 볼륨 다운을 2초 이상 누른다.
   - 4번과 동일하게 동작해야 한다.

6. 백그라운드 녹화 중 기기를 흔든다.
   - 녹화가 종료되지 않아야 한다.

7. 줌 프리셋을 확인한다.
   - `0.6x`, `1x`, `2x`, `3x`, `5x`, `10x` 표시와 실제 동작이 일치해야 한다.
   - mm 렌즈 칩이 없어야 한다.

8. 비율을 확인한다.
   - `3:4`, `9:16`, `Full` preview와 저장 결과가 서로 납득 가능하게 일치해야 한다.
   - 초기 진입 시 preview가 깨지지 않아야 한다.

9. 영상 설정을 확인한다.
   - 해상도/FPS 설정이 실제 녹화 결과에 반영되어야 한다.
   - 지원하지 않는 조합은 선택되지 않아야 한다.
   - 파일 크기 최적화 옵션이 없어야 한다.

10. 사진/영상 모드 UI를 확인한다.
    - 사진 모드에서 마이크 버튼이 보이지 않아야 한다.
    - 영상 모드에서 필요한 경우만 마이크 버튼이 보여야 한다.
    - 촬영 시작/종료 Toast나 팝업성 알림이 불필요하게 뜨지 않아야 한다.

11. PrivateVault를 확인한다.
    - 사진/영상 목록 스크롤이 main thread decode로 버벅이지 않아야 한다.
    - 영상 dialog를 닫으면 playback이 정지되어야 한다.

### ADB 확인 예시

패키지명이 다르면 실제 manifest 기준으로 수정해서 사용한다.

```powershell
adb shell dumpsys activity services com.example.camera2study
adb shell dumpsys media.camera
adb shell cmd notification list
```

## 최종 보고 형식

작업 완료 후 보고에는 아래를 포함한다.

- 생성한 GitHub 이슈 번호
- 커밋 목록과 각 커밋의 핵심 변경
- 빌드/lint 결과
- 실기기 테스트 수행 여부
- 수행하지 못한 테스트와 이유
- 남은 P2/P3 항목이 있다면 명확한 목록
