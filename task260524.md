# task260524.md

2026-05-24 기준 `camera_test` 앱의 Android 16 / One UI 8.0 대응 후속 작업 정의서다.
이 문서는 Claude에게 전달해 바로 작업을 시작할 수 있도록 작성한다.

## 목표

핵심 목표는 **Galaxy S25 Ultra / Android 16 / One UI 8.0 환경에서 백그라운드 녹화 안정성을 유지하면서 Android 16 호환성을 보강**하는 것이다.

이번 작업의 중심은 다음이다.

- 현재 APK에 포함된 CameraX native `.so`의 4KB ELF alignment 문제를 해결한다.
- 백그라운드 녹화 안정성을 우선하므로 CameraX는 최신 대규모 변경 버전보다 보수적 안정 버전인 `1.4.2`로 올린다.
- `compileSdk`는 Android 16 API 36으로 올리되 `targetSdk`는 35로 유지한다.
- 실제 구현이 없는 위치 태그 기능은 완전히 제거한다.
- deprecated API와 잘못된 안내 문구를 정리한다.
- 영상 pause/resume을 구현하되 기존 백그라운드 녹화 종료/Finalize race 방지 구조를 깨지 않는다.

## 현재 상태

- 이슈: [#8 Fix camera lifecycle, S25 Ultra controls, and background recording stability](https://github.com/Won-Hyuk-Lee/camera_test/issues/8)
- 브랜치: `main`
- 현재 HEAD: `0fec383`
- 작업 트리 기준: `problem260524.md`는 untracked 상태일 수 있음
- 기기 기준: Galaxy S25 Ultra / Android 16 API 36 / One UI 8.0
- 현재 앱 설정:
  - `compileSdk = 35`
  - `targetSdk = 35`
  - AGP `8.5.2`
  - Gradle wrapper `8.10`
  - Kotlin plugin `1.9.24`
  - CameraX `1.3.4`

## 작업 방식

기존 지시를 계속 지킨다.

- 현재 브랜치 `main`에서 작업한다.
- 불필요한 브랜치 전환 금지.
- `git add .` 금지.
- 파일을 개별 지정해서 stage한다.
- 작업 단위별로 작게 커밋한다.
- 모든 커밋 본문에 `Issue: #8` 포함.
- `Co-Authored-By` 금지.
- `local.properties`는 로컬 SDK 경로 파일이므로 생성하더라도 커밋하지 않는다.

권장 커밋 단위:

1. `Build`: Android 16 빌드 기반 및 CameraX 1.4.2 업그레이드
2. `Fix`: 위치 태그 미구현 기능 제거 및 설정 문구 정리
3. `Fix`: deprecated API 정리
4. `Feat`: 영상 pause/resume 구현
5. `Docs`: 진행상황 문서 갱신

## 조사로 확정된 사실

현재 `app-debug.apk`에는 CameraX 1.3.4에서 온 것으로 보이는 native 라이브러리가 들어간다.

```text
lib/arm64-v8a/libimage_processing_util_jni.so
lib/armeabi-v7a/libimage_processing_util_jni.so
lib/x86/libimage_processing_util_jni.so
lib/x86_64/libimage_processing_util_jni.so
```

현재 APK의 ELF LOAD segment alignment는 모두 4KB다.

```text
LOAD p_align=[4096, 4096, 4096, 4096]
16KB_ELF=False
```

Android 16에서는 4KB aligned native library가 16KB page size 환경에서 compat mode/경고/안정성 위험 대상이 될 수 있다.
Google Play도 2025-11-01부터 Android 15+ 대상 앱 업데이트에 16KB page size 지원을 요구한다.

CameraX AAR 직접 확인 결과:

- CameraX `1.4.0`: `libimage_processing_util_jni.so` 16KB 정렬 확인
- CameraX `1.4.2`: 16KB 정렬 확인, `libsurface_util_jni.so`도 16KB 정렬
- CameraX `1.5.3`: 16KB 정렬 확인
- CameraX `1.6.1`: 16KB 정렬 확인

이번에는 백그라운드 녹화 안정성이 핵심이므로, 내부 카메라 스택이 크게 바뀐 `1.6.1`보다 보수적으로 `1.4.2`를 선택한다.

## 작업 1. Android 16 빌드 기반 정리

### 목표

Android 16 API 36 기준으로 컴파일할 수 있게 빌드 기반을 정리한다.

### 수정 대상

- `build.gradle.kts`
- `app/build.gradle.kts`
- `gradle/wrapper/gradle-wrapper.properties`
- 필요 시 `gradlew`, `gradlew.bat`

### 작업 내용

- AGP `8.5.2` → `8.9.2`
- Gradle wrapper `8.10` → `8.11.1`
- `compileSdk = 35` → `compileSdk = 36`
- `targetSdk = 35` 유지
- Kotlin plugin은 우선 `1.9.24` 유지
  - 빌드가 Kotlin 버전 때문에 실패할 때만 최소 변경으로 조정한다.
  - 광범위한 Kotlin 2.x / AndroidX 전체 업그레이드는 하지 않는다.

### 환경 주의

현재 로컬에는 `android-36` 및 `build-tools 36.0.0`이 없을 수 있다.

확인:

```powershell
Get-ChildItem "$env:LOCALAPPDATA\Android\Sdk\platforms"
Get-ChildItem "$env:LOCALAPPDATA\Android\Sdk\build-tools"
```

필요하면 SDK Manager 또는 `sdkmanager`로 설치한다.

```powershell
sdkmanager "platforms;android-36" "build-tools;36.0.0"
```

`ANDROID_HOME`이 없으면 빌드가 실패할 수 있다.
빌드 명령에서 임시로 지정하거나 로컬 전용 `local.properties`를 만든다. `local.properties`는 커밋하지 않는다.

```powershell
$env:ANDROID_HOME="$env:LOCALAPPDATA\Android\Sdk"
```

## 작업 2. CameraX 1.4.2 업그레이드

### 목표

CameraX native `.so` 16KB alignment 문제를 해결하되 백그라운드 녹화 안정성을 우선해 `1.4.2`로 보수적 업그레이드한다.

### 수정 대상

- `app/build.gradle.kts`

### 작업 내용

- `val cameraxVersion = "1.3.4"` → `"1.4.2"`
- CameraX artifact 목록은 현재 구조 유지:
  - `camera-core`
  - `camera-camera2`
  - `camera-lifecycle`
  - `camera-video`
  - `camera-view`

### 검증

빌드 후 APK 안 native library가 16KB ELF alignment인지 확인한다.

예시 확인 방식:

```powershell
.\gradlew.bat assembleDebug
jar tf app\build\outputs\apk\debug\app-debug.apk | Select-String "\.so$"
```

`zipalign` 확인:

```powershell
& "$env:LOCALAPPDATA\Android\Sdk\build-tools\36.0.0\zipalign.exe" -v -c -P 16 4 app\build\outputs\apk\debug\app-debug.apk
```

가능하면 ELF LOAD segment의 `p_align`도 확인한다.
`libimage_processing_util_jni.so`와 `libsurface_util_jni.so`가 있다면 모두 `16384` 이상이어야 한다.

## 작업 3. 위치 태그 기능 완전 제거

### 결정

위치 태그 기능은 제거한다.

현재 상태는 UI와 SharedPreferences 저장만 있고 실제 파일 반영이 없다.
사진만 EXIF에 연결하는 것도 반쪽 기능이며, 영상 private_vault 저장 경로에서 MP4 위치 metadata 주입은 CameraX VideoCapture 구조상 별도 구현 부담이 크다.
따라서 이번 작업에서는 기능 자체를 제거한다.

### 제거 대상

- `app/src/main/java/com/example/camera2study/CameraController.kt`
  - `isLocationTagEnabled`
- `app/src/main/java/com/example/camera2study/ui/SettingsBottomSheet.kt`
  - `onLocationTag` callback
  - `initialLocationTag`
  - 위치 태그 초기화/리스너 코드
  - `newInstance()` 파라미터에서 위치 태그 제거
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
  - `currentLocationTag`
  - `onLocationTag` 구현
  - `saveCameraSettings()`의 `last_location_tag`
  - `restoreCameraSettings()`의 `last_location_tag`
- `app/src/main/res/layout/bottom_sheet_settings.xml`
  - `switchLocationTag` 제거

### 완료 조건

- 설정 시트에 위치 태그 스위치가 없다.
- 소스에서 `isLocationTagEnabled`, `last_location_tag`, `switchLocationTag`, `onLocationTag` 검색 결과가 없다.
- 위치 권한은 추가하지 않는다.

## 작업 4. 배터리 최적화 안내 문구 수정

### 목표

앱 표시 이름은 `test`인데 안내 문구에 `camera2study`가 남아 있는 불일치를 정리한다.

### 수정 대상

- `app/src/main/res/layout/fragment_settings.xml`
- `app/src/main/java/com/example/camera2study/ui/SettingsFragment.kt`

### 수정 내용

- `'camera2study' 앱 검색` → `'test' 앱 검색`
- `'camera2study' 앱을 검색해 찾습니다` → `'test' 앱을 검색해 찾습니다`

가능하면 하드코딩 대신 `@string/app_name`을 활용할 수 있는 구조면 그렇게 정리한다.
단, 범위를 키우는 리팩터링은 하지 않는다.

## 작업 5. Deprecated API 정리

### 5-1. `setTargetAspectRatio()` 교체

현재 `CameraController.kt`에서 다음 deprecated API를 사용한다.

- `Preview.Builder().setTargetAspectRatio(...)`
- `ImageCapture.Builder().setTargetAspectRatio(...)`

교체 방향:

- `ResolutionSelector`
- `AspectRatioStrategy`

권장 매핑:

- `RATIO_3_4` → `AspectRatioStrategy.RATIO_4_3_FALLBACK_AUTO_STRATEGY`
- `RATIO_16_9`, `RATIO_FULL` → `AspectRatioStrategy.RATIO_16_9_FALLBACK_AUTO_STRATEGY`

주의:

- 기존 정책은 유지한다.
  - 3:4는 use case 4:3
  - 16:9와 Full은 use case 16:9
  - Full은 preview만 화면을 채우고 저장 결과는 16:9
- `VideoCapture`는 `QualitySelector` 기반이므로 이 교체 대상이 아니다.

### 5-2. `VIBRATOR_SERVICE` 교체

현재 `CameraForegroundService.kt`에서 `Context.VIBRATOR_SERVICE` 사용으로 warning이 난다.

교체 방향:

- API 31 이상:
  - `getSystemService(VibratorManager::class.java).defaultVibrator`
- API 30 이하:
  - 기존 `getSystemService(Context.VIBRATOR_SERVICE) as Vibrator`

주의:

- `minSdk = 26`이므로 버전 분기 필수.
- 볼륨 길게 누르기 종료 시 짧은 진동 동작은 유지한다.

## 작업 6. 영상 pause/resume 구현

### 목표

영상 녹화 중 pause/resume을 앱 내부 UI에서 사용할 수 있게 한다.

CameraX 1.3.4에도 `Recording.pause()` / `Recording.resume()` API와 `VideoRecordEvent.Pause` / `Resume` 이벤트가 존재한다.
CameraX 1.4.2에서도 사용 가능해야 한다.

### 수정 대상

- `app/src/main/java/com/example/camera2study/CameraController.kt`
- `app/src/main/java/com/example/camera2study/CameraForegroundService.kt`
- `app/src/main/java/com/example/camera2study/ui/CameraFragment.kt`
- `app/src/main/res/layout/fragment_camera.xml`

### 구현 요구

`CameraController.kt`:

- `pauseRecording()`
- `resumeRecording()`
- `isPaused()`
- 내부 `isRecordingPaused` 상태 추가
- `VideoRecordEvent.Pause`에서 paused true 반영
- `VideoRecordEvent.Resume`에서 paused false 반영
- `VideoRecordEvent.Finalize`에서 paused false 초기화

`CameraForegroundService.kt`:

- 필요 시 pause/resume 위임 메서드 추가
- 백그라운드 볼륨 길게 누르기 종료 로직과 충돌하지 않게 한다.

`CameraFragment.kt`:

- 녹화 중에만 pause/resume 버튼 표시
- 녹화 전/종료 후에는 버튼 숨김
- pause 상태에서는 버튼이 resume 의미로 바뀌어야 함
- foreground 앱 내부에서만 사용한다.
- 백그라운드 볼륨 종료 정책은 유지한다.

타이머:

- pause 중에는 타이머가 멈춰야 한다.
- resume 시 누적 녹화 시간 기준으로 이어져야 한다.
- 단순히 현재 시간 기준으로 다시 계산해 pause 시간이 녹화 시간에 포함되면 안 된다.

주의:

- `stopRecording()`의 현재 정책을 깨지 않는다.
  - `recording = null`
  - wake lock release
  - service stop
  - 위 정리는 `VideoRecordEvent.Finalize` 이후에 수행
- 반복 녹화 auto-stop과 충돌하지 않게 한다.
  - pause 중 auto-stop 시간을 계속 흘릴지, pause 시간은 제외할지 정책을 코드에서 명확히 한다.
  - 권장: UI 타이머와 동일하게 pause 시간은 녹화 시간에서 제외.
- 촬영 시작/종료 Toast는 다시 추가하지 않는다.

### 완료 조건

- 영상 모드에서 녹화 시작 시 pause/resume 버튼이 보인다.
- pause를 누르면 녹화가 일시정지되고 타이머가 멈춘다.
- resume을 누르면 같은 파일에 녹화가 이어지고 타이머가 이어진다.
- stop 시 파일이 정상 finalize된다.
- 백그라운드 볼륨 2초 종료는 계속 동작한다.

## 작업 7. 검증

### 필수 빌드 검증

```powershell
$env:ANDROID_HOME="$env:LOCALAPPDATA\Android\Sdk"
.\gradlew.bat assembleDebug
.\gradlew.bat lintDebug
```

또는:

```powershell
$env:ANDROID_HOME="$env:LOCALAPPDATA\Android\Sdk"
.\gradlew.bat clean assembleDebug lintDebug
```

### 필수 정적 확인

검색 결과가 없어야 하는 항목:

```powershell
rg "isLocationTagEnabled|last_location_tag|switchLocationTag|onLocationTag" app/src/main
```

deprecated warning이 없어야 하는 항목:

```powershell
rg "setTargetAspectRatio|VIBRATOR_SERVICE" app/src/main
```

CameraX 버전 확인:

```powershell
rg "cameraxVersion" app/build.gradle.kts
```

### 16KB alignment 확인

```powershell
jar tf app\build\outputs\apk\debug\app-debug.apk | Select-String "\.so$"
& "$env:LOCALAPPDATA\Android\Sdk\build-tools\36.0.0\zipalign.exe" -v -c -P 16 4 app\build\outputs\apk\debug\app-debug.apk
```

가능하면 ELF `p_align`도 확인한다.

### S25 Ultra 실기기 검증

실기기 검증은 코드만으로 대체할 수 없다.
Galaxy S25 Ultra / Android 16 / One UI 8.0에서 아래를 확인한다.

1. 카메라 화면 진입 후 녹화하지 않고 앱을 닫는다.
   - foreground service 알림이 남지 않아야 함
   - service 잔존이 없어야 함

2. 영상 녹화 시작 후 홈 이동 30초 이상
   - 저장 파일이 정지 프레임이 아니어야 함

3. 영상 녹화 시작 후 전원 버튼으로 화면 OFF 30초 이상
   - 저장 파일이 정지 프레임이 아니어야 함

4. 백그라운드 녹화 중 볼륨 업 2초 이상
   - 녹화 종료
   - 파일 정상 finalize
   - service 정리

5. 백그라운드 녹화 중 볼륨 다운 2초 이상
   - 4번과 동일하게 동작

6. foreground에서 볼륨 길게 누르기
   - 녹화가 종료되지 않아야 함

7. pause/resume
   - pause 중 타이머 정지
   - resume 후 같은 파일에 이어서 녹화
   - 최종 파일 정상 재생

8. 줌/비율/마이크 UI 회귀
   - 줌 프리셋 `0.6x/1x/2x/3x/5x/10x`
   - 사진 모드에서 마이크 버튼 없음
   - 영상 모드에서 마이크 버튼 표시
   - 비율 초기 깨짐 없음

## 이번 작업에서 하지 않는 것

- CameraX `1.6.1` 업그레이드
  - 이유: 내부 CameraPipe/Media3 Muxer 변화가 커서 백그라운드 녹화 안정성 회귀 가능성이 있다.
  - 필요하면 별도 세션에서 실기기 테스트 포함해 진행한다.
- `targetSdk = 36` 업그레이드
  - 이유: predictive back, edge-to-edge opt-out 제거 등 별도 대응 필요.
- 위치 태그 재구현
- 핀치 줌
- 강제 셔터 무음 우회
- 흔들어서 종료 복구
- Android 시스템 카메라/마이크 사용 표시 숨기기
- 빠른 녹화 0.5초 지연 제거만을 위한 구조 변경

## 최종 보고에 포함할 것

- 변경 커밋 목록
- 각 커밋의 핵심 변경
- 빌드/lint 결과
- 16KB alignment 검증 결과
- S25 Ultra 실기기 검증 수행 여부
- 수행하지 못한 실기기 테스트와 이유
- 남은 위험 또는 후속 작업

