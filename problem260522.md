# 📌 백그라운드 촬영 안전성 및 UI 간소화 개선 과제 (problem260522.md)

갤럭시 S25 울트라 특화 카메라 앱의 백그라운드 위장 촬영 필드 테스트 결과 발견된 오동작 항목 및 UI 불일치 사항을 정리한 핵심 문제점 정의서입니다.

---

### 1. 2.2mm / 6mm 물리 렌즈 선택 칩 (Lens Chip Group) UI 존치 문제
* **문제점**:
  - 현재 앱 하단에 초광각 및 광각 렌즈를 밀리미터(mm) 단위로 선택할 수 있는 동적 칩그룹(`lensChipGroup`)이 배치되어 있습니다.
  - 하지만 이미 구현 완료된 **0.6x, 1.0x, 2.0x 줌 다이얼 버튼**과 **줌 슬라이더**를 통해 갤럭시 S25 울트라의 내부 렌즈 및 줌 배율이 직관적으로 제어되고 있으므로, 굳이 물리 렌즈의 밀리미터 값을 이중으로 고르게 하는 칩 UI는 사용자 편의성을 저해하고 화면 공간을 비효율적으로 차지합니다.
  - 갤럭시 기본 카메라 고유의 심플하고 아름다운 UI 정체성에 위배되는 직관성 방해 요소입니다.

### 2. 백그라운드 전환 및 화면 소등 시 비디오 녹화 프레임 정지(Freeze) 오류
* **문제점 A: `detachPreview()`의 프리뷰 Surface 공급 단절로 인한 카메라 세션 붕괴**
  - 앱이 백그라운드로 가거나 전원 버튼을 눌러 화면을 소등하면 프래그먼트 생명주기에 의해 `onDestroyView()`가 작동하며 `controller.detachPreview()`가 호출됩니다.
  - 이 내부에서 `previewUseCase?.setSurfaceProvider(null)`을 강제로 실행하는데, 동영상 촬영이 정상 작동 중(`isRecording() == true`)인 경우에도 프리뷰 서피스를 떼어내 버리기 때문에 CameraX 디바이스가 카메라 캡처 세션을 재구성(Reconfiguration)하게 됩니다.
  - 이로 인해 활성화되어 있던 `VideoCapture` 파이프라인의 프레임 전송 통로가 함께 강제 차단되거나 일시정지 상태로 고착되어, 녹화가 끊기지는 않으나 비디오 프레임이 특정 장면에 굳어버린 **정지 화면(Freeze)**으로 녹화되는 심각한 오류를 야기합니다.

* **문제점 B: Android 14 백그라운드 오디오(마이크) 획득 권한 차단**
  - 동영상 녹화 파이프라인은 비디오 스트림과 마이크 오디오 스트림이 실시간으로 결합(Muxing)되어 돌아갑니다.
  - 현재 `AndroidManifest.xml` 내에 포그라운드 서비스의 타입으로 `camera`만 기재되어 있고, **`microphone` 타입 및 관련 `FOREGROUND_SERVICE_MICROPHONE` 권한이 누락**되어 있습니다.
  - 이로 인해 디바이스가 백그라운드로 진입하거나 화면이 꺼지는 즉시 Android 14 백그라운드 제약 정책에 의해 마이크 오디오 접근이 강제로 묵음(Silence) 차단당하게 되며, 이에 따라 전체 녹화 라이터(Writer) 세션이 대기 상태로 고정되는 병목이 일어납니다.
